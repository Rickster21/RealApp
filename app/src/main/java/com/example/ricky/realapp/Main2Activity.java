package com.example.ricky.realapp;

import android.app.Activity;
import android.os.Bundle;

public class Main2Activity extends Activity {

    //these items below are what you need to make the visuals for the second activity

  /*  ListView mListView;
    String[] speciesNames = {"Black Widow", "Brown", "Cholo"};  // These are the strings to describe the custom list's pics below in the species pics
    int[] speciesPics ={R.drawable.blackwido, R.drawable.brown,R.drawable.cholo}; // the pics for the custom list


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);

        mListView = (ListView) findViewById(R.id.listview);
        MyApadter myApadter = new MyApadter(Main2Activity.this,speciesNames,speciesPics);
        mListView.setAdapter(myApadter);


    }*/


    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main2);


    }

}
